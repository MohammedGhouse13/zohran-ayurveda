import {NextResponse} from "next/server"; import {createClient} from "@/lib/supabase/server"; export async function GET(){const {data:{user}}=await (await createClient()).auth.getUser();return NextResponse.json({user:user?{id:user.id,email:user.email,phone:user.phone}:null})}
