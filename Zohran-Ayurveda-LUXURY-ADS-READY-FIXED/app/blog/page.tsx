import Image from "next/image"; import Link from "next/link"; import {createClient} from "@/lib/supabase/server";
export default async function BlogPage(){const supabase=await createClient();const {data}=await supabase.from("blog_posts").select("*").eq("is_published",true).order("published_at",{ascending:false});return <section className="section2"><div className="container"><div className="sectionhead2"><div><div className="kicker">THE ZOHRAN JOURNAL</div><h1>Stories behind the brand.</h1></div><p>Founder notes, Ayurvedic perspectives and the journey of Zohran Ayurveda.</p></div><div className="bloggrid">{(data||[]).map(p=><Link className="blogcard" href={`/blog/${p.slug}`} key={p.id}>{p.cover_image_url&&<div className="blogcover"><Image src={p.cover_image_url} alt="" fill sizes="(max-width:900px) 100vw, 33vw"/></div>}<div className="blogbody"><small>{new Date(p.published_at||p.created_at).toLocaleDateString("en-IN")}</small><h3>{p.title}</h3><p>{p.excerpt||p.content.slice(0,150)}</p><span>Read story →</span></div></Link>)}</div>{!data?.length&&<div className="notice">The journal is coming soon.</div>}</div></section>}
